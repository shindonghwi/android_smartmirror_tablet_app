package orot.apps.sognora_websocket_audio

import okio.ByteString
//import orot.apps.sognora_websocket_audio.model.WebSocketState

//interface AudioStreamManageable {
//    suspend fun stateWebSocket(state: WebSocketState) // 웹 소켓의 상태
//    suspend fun stateAudioStream(isAvailable: Boolean) // 오디오 스트림 사용상태
//    suspend fun receivedMessageString(msg: String)
//    suspend fun receivedMessageByteString(byte: ByteString)
//    suspend fun streamAiTalk(id: String, receivedMsg: MessageProtocol)
//    suspend fun saidMe(id: String, receivedMsg: MessageProtocol)
//}